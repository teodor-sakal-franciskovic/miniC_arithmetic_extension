//isti naziv za dve lambda funkcije (baca se greska iako nije isti broj parametara)

int main(){
  
  int a;
  
  lambda x = int b, int c, int g : c + b + g;
  lambda x = int a, int d, int f : a * d * f + 2;
  return 2;
}
